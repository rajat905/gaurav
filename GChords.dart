import 'package:flutter/material.dart';

class GChord extends StatelessWidget {
  const GChord({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}